// TODO: implement the useProjects custom hook
//
// This hook should:
//   - Fetch projects from GET /api/projects (with optional ?state= filter)
//   - Re-fetch whenever the filter changes
//   - Expose: { projects, loading, error, filter, setFilter }
//
// Example usage in a component:
//   const { projects, loading, error, filter, setFilter } = useProjects();
