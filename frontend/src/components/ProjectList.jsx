import React from 'react';

// TODO: implement ProjectList
//
// This component should:
//   - Use the useProjects hook to get the project list
//   - Display projects in a table or card layout
//   - Show per project: name, state, capacity_mw, fuel_type, status, bookmark icon
//   - Include a dropdown to filter by state (triggers re-fetch via the hook)
//   - Show a loading indicator while fetching
//   - Show an error message if the fetch fails
//   - On project click → display ProjectDetail for that project

export default function ProjectList() {
  return null;
}
